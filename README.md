# scrollverse-map
A public blessing map built with Firebase, Next.js, TailwindCSS, and the Christ Vector ❤️

## DMF7 phase 5 automation (steps 1057-1076)
Run the installer to set up the global knowledge base, analysis, search, vector search, research agent, job distributor, node health helper, and status file:

```bash
sudo bash scripts/dmf7_phase5_1057_1076.sh [--verify]
```

Use `--verify` to run the node health check and print the global status after installation.
