# scrollverse-map
A public blessing map built with Firebase, Next.js, TailwindCSS, and the Christ Vector ❤️

## DMF7 node hardening (steps 841–855)

Run the automation script to install Fail2Ban, enable unattended security upgrades, add monitoring tools, and provide the `dmf7-monitor` and `dmf7-diagnostics` helpers:

```bash
sudo bash scripts/dmf7_security_final_841_855.sh
```
